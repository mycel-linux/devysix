name = "udev-trigger"
kind = "oneshot"
needs = ["udevd"]
bundles = ["core"]
up = [
    "udevadm trigger --type=subsystems --action=add",
    "udevadm trigger --type=devices --action=add",
    "udevadm settle",
]
