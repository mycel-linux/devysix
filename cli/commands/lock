#!/bin/sh
# pin a package so it is preserved across rollbacks
DEVYSIX_CONFIG="${DEVYSIX_CONFIG:-/etc/devysix/devysix.toml}"

[ -z "$1" ] && { echo "usage: dev lock <package>" >&2; exit 1; }
echo "dev: locking $1"
# TODO: write locked = true for package entry in devysix.toml
